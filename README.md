# 台灣金銀價格追蹤與分析系統 🏆

基於多Agent架構的智能金銀價格追蹤系統，每2分鐘自動更新價格並使用Gemini AI進行綜合分析。

## 🌟 系統特色

- **📊 即時價格追蹤**：每2分鐘自動更新台灣金銀價格（以一錢為單位）
- **📈 統計分析**：自動計算月度平均價格、最高/最低價格、標準差等指標
- **🤖 AI智能分析**：使用Gemini 3進行市場分析、趨勢預測和投資建議
- **🎨 精美圖表**：基於ECharts的互動式價格趨勢圖表
- **⚡ 自動刷新**：前端每2分鐘自動刷新數據

## 🏗️ 多Agent架構

系統由5個專門的Agent協同工作：

1. **數據採集器 (Data Collector Agent)**
   - 從外部API獲取金銀價格
   - 數據驗證和清洗
   - 每2分鐘自動執行

2. **數據分析器 (Data Analyzer Agent)**
   - 計算月度平均價格
   - 統計分析和趨勢計算
   - 異常檢測

3. **AI分析器 (AI Analyzer Agent)**
   - 使用Gemini 3進行綜合分析
   - 生成市場洞察和預測
   - 提供投資建議

4. **可視化代理 (Visualization Agent)**
   - 提供API端點供前端調用
   - 數據格式化

5. **協調器 (Agent Coordinator)**
   - 協調各Agent工作流程
   - 管理定時任務
   - 錯誤處理

## 🛠️ 技術棧

### 後端
- **Python 3.11+**
- **FastAPI** - 高性能Web框架
- **SQLAlchemy** - ORM數據庫
- **Google Gemini AI** - AI分析
- **APScheduler** - 任務調度
- **NumPy/Pandas** - 數據分析

### 前端
- **React 18** - UI框架
- **Vite** - 構建工具
- **ECharts** - 圖表庫
- **Axios** - HTTP客戶端

### 數據庫
- **SQLite** (開發) / **PostgreSQL** (生產)

## 📦 快速開始

### 1. 環境準備

確保已安裝：
- Python 3.11+
- Node.js 18+
- npm 或 yarn

### 2. 配置環境變數

複製環境變數範本：
```bash
cp .env.example .env
```

編輯 `.env` 文件，填入您的配置：
```bash
# Gemini AI API Key（必須）
GEMINI_API_KEY=your_gemini_api_key_here

# 其他配置使用默認值即可
```

### 3. 啟動後端

```bash
cd backend

# 安裝依賴
pip install -r requirements.txt

# 啟動服務器
python main.py
```

後端將在 `http://localhost:8000` 運行

API文檔：http://localhost:8000/docs

### 4. 啟動前端

開啟新終端：

```bash
cd frontend

# 安裝依賴
npm install

# 啟動開發服務器
npm run dev
```

前端將在 `http://localhost:5173` 運行

### 5. 訪問系統

打開瀏覽器訪問：http://localhost:5173

## 📡 API端點

### 價格相關
- `GET /api/` - 系統首頁
- `GET /api/health` - 健康檢查
- `GET /api/prices/current` - 獲取當前價格
- `GET /api/history?days=30` - 獲取歷史數據

### 分析相關
- `GET /api/statistics/monthly` - 獲取月度統計
- `GET /api/ai-analysis/latest` - 獲取最新AI分析
- `GET /api/latest` - 獲取所有最新數據

### 操作相關
- `POST /api/collect` - 手動觸發數據採集

## 🔧 配置說明

### 環境變數

| 變數名 | 說明 | 默認值 |
|--------|------|--------|
| `GEMINI_API_KEY` | Gemini AI API密鑰 | 必填 |
| `REFRESH_INTERVAL` | 刷新間隔（秒） | 120 |
| `DATABASE_URL` | 數據庫連接URL | sqlite:///./gold_silver.db |
| `LOG_LEVEL` | 日誌級別 | INFO |

### 獲取Gemini API Key

1. 訪問 [Google AI Studio](https://makersuite.google.com/app/apikey)
2. 登入您的Google帳號
3. 創建新的API Key
4. 將API Key複製到 `.env` 文件

## 📊 數據流程

```
外部API → Data Collector → Database
                ↓
         Data Analyzer → Statistics
                ↓
          AI Analyzer → AI Analysis
                ↓
        Visualization → Frontend
```

## 🎯 主要功能

### 1. 即時價格追蹤
- 每2分鐘自動採集最新金銀價格
- 顯示即時價格和更新時間
- 歷史價格趨勢圖表

### 2. 統計分析
- 月度平均價格
- 最高/最低價格
- 價格波動範圍
- 標準差分析

### 3. AI智能分析
- 市場分析：評估當前市場狀況
- 趨勢預測：預測未來3-7天價格走勢
- 投資建議：針對不同風險偏好提供建議
- 風險提示：識別市場風險因素

### 4. 數據可視化
- 金銀價格雙軸折線圖
- 漸變填充效果
- 互動式圖表縮放
- 響應式設計

## 🐛 故障排除

### 後端無法啟動
- 檢查Python版本：`python --version`（需要3.11+）
- 檢查依賴安裝：`pip list`
- 檢查端口佔用：`lsof -i :8000`

### 前端無法啟動
- 檢查Node版本：`node --version`（需要18+）
- 清除依賴重裝：`rm -rf node_modules && npm install`
- 檢查端口佔用：`lsof -i :5173`

### AI分析不工作
- 檢查Gemini API Key是否正確
- 查看後端日誌確認錯誤信息
- 確認網絡連接正常

## 📝 注意事項

> [!IMPORTANT]
> - 當前系統使用**模擬數據**，實際部署需要整合真實的台灣金銀價格API
> - Gemini API需要網絡連接，請確保服務器可以訪問Google服務
> - 建議在生產環境使用PostgreSQL替代SQLite

## 🚀 部署建議

### 生產環境配置

1. **使用PostgreSQL**
   ```bash
   DATABASE_URL=postgresql://user:password@localhost:5432/gold_silver_db
   ```

2. **使用Gunicorn運行後端**
   ```bash
   gunicorn backend.main:app -w 4 -k uvicorn.workers.UvicornWorker
   ```

3. **前端構建**
   ```bash
   cd frontend
   npm run build
   ```

4. **使用Nginx反向代理**

## 📄 許可證

MIT License

## 👥 貢獻

歡迎提交Issue和Pull Request！

## 📧 聯繫方式

如有問題或建議，請提交Issue。
